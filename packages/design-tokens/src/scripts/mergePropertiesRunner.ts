// NOTE: For ts-node to run ESModules, the `mergeProperties` TS file needs to be imported as `.js`. See https://github.com/TypeStrong/ts-node/issues/1007.
import mergeProperties from "../mergeProperties.js";

mergeProperties();

export {};
