{
    "name": "root",
    "private": true,
    "scripts": {
        "bootstrap": "npm install && lerna exec -- npm install && lerna link --force-local"
    },
    "devDependencies": {
        "lerna": "^3.22.1",
        "prettier": "^2.1.2"
    }
}
